connections {
  remote-main {
    local_addrs  = 10.0.0.4
    remote_addrs = 10.0.0.2
    local {
      auth = pubkey
      certs = remote_cert.pem
      id = "CN=remote.heig.ch, O=HEIG, C=CH"
    }
    remote {
      auth = pubkey
      id = "CN=main.heig.ch, O=HEIG, C=CH"
    }
    children {
      remote-main {
        local_ts  = 10.0.0.4/32
        remote_ts = 10.0.1.0/24, 10.0.2.0/24
        start_action = start
      }
    }
  }
}
