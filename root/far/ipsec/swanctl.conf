connections {
  far-main {
    local_addrs  = 10.0.0.3
    remote_addrs = 10.0.0.2
    local {
      auth = pubkey
      certs = far_cert.pem
      id = "CN=far.heig.ch, O=HEIG, C=CH"
    }
    remote {
      auth = pubkey
      id = "CN=main.heig.ch, O=HEIG, C=CH"
    }
    children {
      far-main {
        local_ts  = 10.0.2.0/24
        remote_ts = 10.0.1.0/24, 10.0.0.4/32
        start_action = trap
      }
    }
  }
}
